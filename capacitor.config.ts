import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.khashawi.therapod',
  appName: 'therapod-report-ios',
  webDir: 'dist'
};

export default config;
